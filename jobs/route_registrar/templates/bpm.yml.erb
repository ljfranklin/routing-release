---
processes:
  route-registrar:
    executable: /var/vcap/jobs/route_registrar/bin/route-registrar
    args:
      - -pidFile=/var/vcap/data/route_registrar.pid
      - -configPath=/var/vcap/jobs/route_registrar/config/registrar_settings.yml
