---
processes:
  route_registrar:
    executable: /var/vcap/packages/route_registrar/bin/route-registrar
    args:
      - -pidfile=/var/vcap/data/route_registrar/route_registrar.pid
      - -configPath=/var/vcap/jobs/route_registrar/config/registrar_settings.yml
